name: juste_a_temps_domain
description: "Domaine artificiel du POC LOT 0 (INTERVENTION_ALPHA), en Dart pur."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.13.3

resolution: workspace

# Dart pur : aucune dépendance d'exécution, ni Flutter ni dart:io
# (doc/dev/architecture/LOT0_POC_CONCEPTION.md §3.2).
dependencies: {}
dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
